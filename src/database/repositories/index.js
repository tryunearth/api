const AuthRepo = require('./auth')
const RedditRepo = require('./reddit')
const TagsRepo = require('./tags')
const ThingsRepo = require('./things')

module.exports = {
  AuthRepo,
  RedditRepo,
  TagsRepo,
  ThingsRepo,
}
